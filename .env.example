# ClawTrade-BNB Environment Configuration
# Copy this file to .env and fill in your values

# ⚠️ SECURITY: Never commit real private keys. Use testnet keys only for demo.
# For production: use hardware wallets or secure key management.

# Blockchain Configuration
PRIVATE_KEY=6d816d363a599dd5e09b12d2ebd4aa0cb522d52da3ec02b64dd1b29e1ce62989
RPC_URL=https://bsc-testnet.publicnode.com
CHAIN_ID=97
NETWORK=testnet

# Network: testnet or mainnet
# NETWORK=mainnet

# Port Configuration
AGENT_PORT=3001
UI_PORT=5173

# Agent Configuration
OPERATOR_MODE=auto_execute
# Options: auto_execute (execute TX), suggest_only (show proposals only)

RISK_PROFILE=balanced
# Options: conservative (safe), balanced (default), aggressive (high-yield)

# Optional: Custom BscScan URL
BSCSCAN_URL=https://testnet.bscscan.com

# Optional: Log file path
LOG_PATH=./execution-log.jsonl

# Optional: Cycle interval (seconds)
CYCLE_INTERVAL=60

# Optional: Enable demo mode (uses env wallet, doesn't require wallet connection)
DEMO_MODE=true
